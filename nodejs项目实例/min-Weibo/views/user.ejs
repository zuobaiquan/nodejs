<%- include header %>
    <% if (user) { %>   
    <% } %>
 <form role="form" method="post">
  <div class="form-group">
    <label for="post">username</label>
    <input name="post" type="text" class="form-control" id="username" placeholder="Enter username">
  </div>
  <button type="submit" class="btn btn-default">发言</button>
</form>
<% posts.forEach(function(post, index) {
      if (index % 3 == 0) { %>
        <div class="row">
      <%} %>
          <div class="col-md-4">
            <h2>
               <a href="/u/<%= post.user %>"><%= post.user %></a>
               说
            </h2> 
             <p><small><%= post.time %></small></p>
             <p><%= post.post %></p>
          </div>
             <% if (index % 3 == 2) { %>
        </div>
             <!-- end row -->
        <% } %>
<%}) %>
<% if (posts.length % 3 != 0) { %>
  </div>
  <!-- end row -->
<%} %>
<%- include footer %>

  